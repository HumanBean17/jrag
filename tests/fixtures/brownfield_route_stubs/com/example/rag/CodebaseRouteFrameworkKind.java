package com.example.rag;

/** Test stub — mirrors `VALID_ROUTE_FRAMEWORKS` strings. */
public enum CodebaseRouteFrameworkKind {
    spring_mvc,
    webflux,
    feign,
    kafka,
    rabbitmq,
    jms,
    stream
}
