package com.example.rag;

/** Test stub — mirrors `VALID_ROUTE_KINDS` strings. */
public enum CodebaseRouteKind {
    http_endpoint,
    http_consumer,
    kafka_topic,
    rabbit_queue,
    jms_destination,
    stream_binding
}
