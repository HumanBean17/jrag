# Optional: separate venv only for bare `cocoindex` CLI experiments or cross-bundle
# indexing without installing a full bundle. For normal use, install the `requirements.txt`
# inside the bundle you use — each file now includes CocoIndex (version line matches that
# bundle’s flows: 1.0.x for SQLite / Lance, 0.3.x for Postgres / Chroma).
#
#   python3 -m venv .venv-indexer
#   .venv-indexer/bin/pip install -r requirements-indexer.txt
#   export PATH="$(pwd)/.venv-indexer/bin:$PATH"

cocoindex>=1.0.0
