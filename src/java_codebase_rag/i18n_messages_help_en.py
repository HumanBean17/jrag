"""English argparse-help catalog (HELP_* keys).

Extracted from the pre-i18n literals (byte-exact) by the Task 10 migration;
shared flag strings share one key across parsers and files. Sequentially
numbered ``HELP_MISC_N`` keys carry description/epilog blocks with no natural
flag or command anchor — the number is the stability contract; renaming to a
mnemonic is safe only in the same commit as its call site.
"""
from __future__ import annotations

from typing import Any

MESSAGES: dict[str, Any] = {
    'HELP_DESC_CLI_MAIN': "jrag — graph-native code intelligence for Java microservices.\n\nLifecycle commands stream subprocess progress to stderr (including relayed child stdout); --quiet suppresses that stream; stdout remains the machine-readable payload.\n\nLifecycle (manage the index):\n  init            Create a fresh index from a Java repository.\n  increment       Pick up changes since the last index update (Lance + graph).\n  reprocess       Full vector + graph rebuild (default); optional --vectors-only / --graph-only.\n  erase           Delete the index from disk.\n\nIntrospection (inspect the index):\n  meta            Print ontology version, edge counts, and table summary.\n  tables          List Lance tables and row counts.\n  diagnose-ignore Show which ignore-pattern layer decided a path's fate.\n  unresolved-calls  List or aggregate receiver-failure call sites (not in CALLS).\n\nAnalysis (work with code changes):\n  analyze-pr      Compute blast-radius + risk score for a unified diff.\n\nRun `jrag <command> --help` for command-specific options.",
    'HELP_DESC_JRAG_MAIN': 'jrag - agent-facing CLI for graph-native code intelligence.\n\nEvery <query> command resolves the identifier (FQN / simple name /\nroute path / topic) as the first step and maps one/many/none onto a\nsingle envelope. Default output is compact text; `--format json` emits\nthe envelope verbatim.\n\nCommands by group:\n  health:      status\n  locate:      find, inspect\n  listings:    routes, clients, producers, topics, jobs, listeners,\n               entities\n  traversal:   callers, callees, hierarchy, implementations, subclasses,\n               overrides, overridden-by, dependents, impact, decompose,\n               flow, dependencies, connection, outline, imports\n  orientation: microservices, map, conventions, overview\n  search:      search\n\nRun `jrag <command> --help` for command-specific options.',
    'HELP_ARG_FILE': 'File path as stored in the graph (POSIX-relative to source root).',
    'HELP_ARG_MICROSERVICE': 'Microservice NAME (literal — NOT resolved as a query).',
    'HELP_ARG_QUERY': 'Search query (name/FQN). Omit for filter mode.',
    'HELP_ARG_SUBJECT': "Microservice name, route path (starts with '/'), or topic string.",
    'HELP_CMD_ANALYZE_PR': 'Blast-radius + risk score for a unified diff.',
    'HELP_CMD_CALLEES': 'What does this symbol call?',
    'HELP_CMD_CALLERS': 'Who calls this symbol or route?',
    'HELP_CMD_CONNECTION': 'Cross-service connections for a microservice (inbound/outbound).',
    'HELP_CMD_CONVENTIONS': 'Dominant roles + framework tallies.',
    'HELP_CMD_DECOMPOSE': 'Role-waterfall flow from an entrypoint.',
    'HELP_CMD_DEPENDENCIES': 'Types this Symbol injects (INJECTS out).',
    'HELP_CMD_DEPENDENTS': 'Who injects this type?',
    'HELP_CMD_DIAGNOSE_IGNORE': 'Show which ignore-pattern layer decided the fate of a path.',
    'HELP_CMD_ENTITIES': 'List JPA entities.',
    'HELP_CMD_ERASE': 'Delete the index from disk.',
    'HELP_CMD_FIND': 'Find nodes by query or filter.',
    'HELP_CMD_FLOW': 'Request flow through a route (inbound callers + outbound CALLS hops).',
    'HELP_CMD_HIERARCHY': 'Type hierarchy (parents and children).',
    'HELP_CMD_HTTP_CLIENTS': 'List HTTP clients.',
    'HELP_CMD_HTTP_ROUTES': 'List HTTP routes.',
    'HELP_CMD_IMPACT': 'Fleet-wide blast radius (INJECTS/IMPLEMENTS/EXTENDS reverse closure).',
    'HELP_CMD_IMPLEMENTATIONS': 'Classes implementing an interface.',
    'HELP_CMD_IMPORTS': 'List imports declared in a file (tree-sitter parse + resolve_v2).',
    'HELP_CMD_INCREMENT': 'Pick up changes since the last index update.',
    'HELP_CMD_INIT': 'Create a fresh index from a Java repository.',
    'HELP_CMD_INSPECT': 'Inspect a node by query.',
    'HELP_CMD_INSTALL': 'Interactive setup wizard: config, agent-surface registration, indexing.',
    'HELP_CMD_JOBS': 'List scheduled tasks.',
    'HELP_CMD_LIST': 'List unresolved call sites.',
    'HELP_CMD_LISTENERS': 'List message listeners.',
    'HELP_CMD_MAP': 'Symbol counts per kind, grouped by service or module.',
    'HELP_CMD_META': 'Print graph meta and embedding resolution.',
    'HELP_CMD_MICROSERVICES': 'List microservices with resolved type counts.',
    'HELP_CMD_OUTLINE': 'List symbols declared in a file.',
    'HELP_CMD_OVERRIDDEN_BY': 'Methods overriding this one (dispatch DOWN to overriders).',
    'HELP_CMD_OVERRIDES': 'Methods this method overrides (dispatch UP to declaration).',
    'HELP_CMD_OVERVIEW': 'Bundle for a microservice, route, or topic.',
    'HELP_CMD_PRIME': 'Print agent priming context (index state + command surface).',
    'HELP_CMD_PRODUCERS': 'List async message producers.',
    'HELP_CMD_REPROCESS': 'Rebuild vectors and/or LadybugDB (default: both full phases).',
    'HELP_CMD_SEARCH': 'Semantic search over the indexed JVM sources.',
    'HELP_CMD_STATS': 'Aggregate unresolved call site counts.',
    'HELP_CMD_STATUS': 'Print index freshness, ontology version, and counts.',
    'HELP_CMD_SUBCLASSES': 'Classes extending a type.',
    'HELP_CMD_TABLES': 'List Lance tables and row counts.',
    'HELP_CMD_TOPICS': 'List message topics (producer-grouped).',
    'HELP_CMD_UNRESOLVED_CALLS': 'List or aggregate UnresolvedCallSite rows (receiver-failure call sites).',
    'HELP_CMD_UPDATE': 'Refresh the deployed surface (MCP entry / prime hook) after pip upgrade.',
    'HELP_CMD_VOCAB_INDEX': 'Rebuild the vocabulary index (absence diagnosis).',
    'HELP_CMD_WATCH': 'keep the index fresh and serve warm queries while running',
    'HELP_FLAG_AGENT': 'Agent host to configure (can be passed multiple times).',
    'HELP_FLAG_ANNOTATION': 'Filter by annotation.',
    'HELP_FLAG_AS': 'Override auto-detection of subject type.',
    'HELP_FLAG_BACKEND': 'File-watch backend (overrides YAML `watch:backend`).',
    'HELP_FLAG_BOTH': 'Show both inbound and outbound sections (this is the default).',
    'HELP_FLAG_BY': "Grouping axis: microservice (default) or module. When --module is set without --by, the axis defaults to module (the user's focus is the module axis); pass --by microservice to keep microservice grouping.",
    'HELP_FLAG_CALLS_PATH_CONTAINS': 'Filter by target path substring (client).',
    'HELP_FLAG_CALLS_SERVICE': 'Filter by target service (client).',
    'HELP_FLAG_CAPABILITY': 'Filter by capability.',
    'HELP_FLAG_CHUNKS': 'Show every chunk (default collapses to one row per symbol/type).',
    'HELP_FLAG_CLIENT_KIND': 'Filter by client kind (client).',
    'HELP_FLAG_CONSUMER_IN': 'Show consumers from this microservice.',
    'HELP_FLAG_COUNT': 'Print only the result count (no rows) — bare int in text, {{"status","count"}} in json. Counts nodes (listing), edges (traversal), or 1 (inspect).',
    'HELP_FLAG_DEBOUNCE_MS': 'Reindex debounce window in ms (overrides YAML `watch:debounce_ms`).',
    'HELP_FLAG_DEPTH': 'Call-graph depth (default 1).',
    'HELP_FLAG_DETACH': 'Start the daemon as a detached background process and return.',
    'HELP_FLAG_DETAIL': 'Output detail level (default normal) — ORTHOGONAL to --format: both text and json honor it. brief = identity only (name @service); normal = +module/role/file/score; full = +signature/annotations/snippet.',
    'HELP_FLAG_DRY_RUN': 'Print changes without writing files.',
    'HELP_FLAG_EMBEDDING_DEVICE': 'Override SBERT_DEVICE / YAML embedding.device',
    'HELP_FLAG_EMBEDDING_MODEL': 'Override SBERT_MODEL / YAML embedding.model',
    'HELP_FLAG_EXCLUDE_ROLE': 'Exclude by role.',
    'HELP_FLAG_EXISTS': 'Print only an exists boolean (true/false, or {{"status","exists"}} in json). Exit 0 when results exist, 2 otherwise (incl. resolve miss / empty result).',
    'HELP_FLAG_EXPLAIN': 'Show score breakdown per hit.',
    'HELP_FLAG_FIELDS': 'Comma-separated node-field allowlist that overrides --detail (e.g. fqn,role,signature). Ignored with --count/--exists; primarily a --format json lever; text still labels rows from whatever identity fields survive.',
    'HELP_FLAG_FOLLOW_CALLS': 'Top up each stage with DECLARES+CALLS type-to-type hops when the structural INJECTS/EXTENDS/IMPLEMENTS pass under-fills it (default: on). --no-follow-calls restricts the waterfall to structural edges.',
    'HELP_FLAG_FORCE': 'Overwrite all artifacts even if content matches.',
    'HELP_FLAG_FORMAT': 'Output format (default: text).',
    'HELP_FLAG_FQN_CONTAINS': 'Filter by FQN substring.',
    'HELP_FLAG_FRAMEWORK': 'Filter by framework.',
    'HELP_FLAG_FUZZY': 'Query mode: fall back from exact name/FQN to prefix then substring (case-sensitive) when the exact match is empty.',
    'HELP_FLAG_GRAPH_ONLY': 'Run only build_ast_graph.py (no cocoindex / Lance reprocess).',
    'HELP_FLAG_HOOK_JSON': 'Wrap output in the SessionStart hook JSON envelope.',
    'HELP_FLAG_HTTP_METHOD': 'Filter by HTTP method (route).',
    'HELP_FLAG_HYBRID': 'Enable vector+keyword hybrid search.',
    'HELP_FLAG_INBOUND': 'Show only inbound connections (default is --both).',
    'HELP_FLAG_INCLUDE_EXTERNAL': 'Include external (JDK/Spring/Lombok) callers/callees (default excluded).',
    'HELP_FLAG_INDEX_DIR': 'Index directory override (default: discovered from cwd).',
    'HELP_FLAG_JAVA_KIND': 'Filter by Java symbol kind.',
    'HELP_FLAG_KIND': 'Node kind (omit for auto-inference from domain flags).',
    'HELP_FLAG_LANG': 'Interface language for output, help, and errors (default: en; also JAVA_CODEBASE_RAG_LANGUAGE or YAML language:).',
    'HELP_FLAG_LIMIT': 'Cap on results (default 20).',
    'HELP_FLAG_METHOD': 'Filter by HTTP method.',
    'HELP_FLAG_METHOD_ID': 'Caller Symbol id',
    'HELP_FLAG_MIN_CONFIDENCE': 'Minimum CALLS edge confidence in [0.0, 1.0].',
    'HELP_FLAG_MIN_SCORE': 'Drop hits with a relevance score below this floor. Default 0.0 drops negative-score noise (chunks farther than orthogonal to the query); raise to tighten precision.',
    'HELP_FLAG_MODEL': "Embedding model path or 'auto' (default: auto).",
    'HELP_FLAG_MODULE': 'Filter by module.',
    'HELP_FLAG_NON_INTERACTIVE': 'Run without prompts (requires --agent).',
    'HELP_FLAG_NO_AUTO_SCOPE': 'Disable cwd-derived auto --service scoping so cross-service results are visible (also disabled via JRAG_NO_AUTO_SCOPE=1).',
    'HELP_FLAG_OFFSET': 'Page offset (filter mode only; ignored in query mode).',
    'HELP_FLAG_OUTBOUND': 'Show only outbound connections (default is --both).',
    'HELP_FLAG_PATH_CONTAINS': 'Filter by path substring (route).',
    'HELP_FLAG_PER_STAGE_LIMIT': 'Cap on symbols per stage (stage_limit, default 20). Not a stage-count knob.',
    'HELP_FLAG_PRODUCER_IN': 'Scope producers to this microservice.',
    'HELP_FLAG_PRODUCER_KIND': 'Filter by producer kind (producer).',
    'HELP_FLAG_QUIET': 'Suppress stderr progress relay; stdout payload unchanged.',
    'HELP_FLAG_REASON': 'Filter by UnresolvedCallSite.reason',
    'HELP_FLAG_RETRIEVAL': "Retrieval mode: 'vectors' (semantic search; requires an embedding model — auto-downloaded from Hugging Face or a local path) or 'bm25' (keyword search; no model, no downloads, works offline). Default: vectors.",
    'HELP_FLAG_ROLE': 'Filter by role.',
    'HELP_FLAG_SCOPE': 'Installation scope (default: project).',
    'HELP_FLAG_SERVICE': 'Filter by microservice.',
    'HELP_FLAG_SOURCE_LAYER': 'Filter by source layer.',
    'HELP_FLAG_SOURCE_ROOT': 'Java repository root (default: cwd)',
    'HELP_FLAG_STATUS': 'Print whether the daemon is up or down and exit.',
    'HELP_FLAG_STOP': 'Stop a running daemon (SIGTERM; SIGKILL after 5s).',
    'HELP_FLAG_SURFACE': "Agent surface to install: 'cli' (jrag console-script + SessionStart prime hook, no files deployed) or 'mcp' (stdio MCP server entry, tools only, no skill/agent artifacts). Omit to choose interactively; non-interactive mode defaults to 'cli'.",
    'HELP_FLAG_TOPIC_CONTAINS': 'Filter by topic substring (producer).',
    'HELP_FLAG_VECTORS_ONLY': 'Run only cocoindex catch-up (Lance); skip graph update.',
    'HELP_FLAG_VERBOSE': 'Show full subprocess output (Lance warnings, brownfield events, progress bars).',
    'HELP_FLAG_YES': 'Confirm destructive deletion (required in CI)',
    'HELP_MISC_1': 'Index health and freshness. Reports ontology version, source root, built_at, parse_errors, edge counts, and the counts dictionary from GraphMeta. Exits 2 with an actionable envelope if the index is missing or stale. An aggregate view: --service / --module / --limit are NOT accepted (rejected at parse time).',
    'HELP_MISC_10': 'List HTTP routes by microservice, framework, path substring, or method. Returns route nodes (no resolve step). HTTP-server-route surface only — kafka topics live under `topics`.',
    'HELP_MISC_11': 'Filter by path substring.',
    'HELP_MISC_12': 'List HTTP clients by microservice, client kind, target service, or path substring. Returns client nodes (no resolve step).',
    'HELP_MISC_13': 'Filter by client kind.',
    'HELP_MISC_14': 'Filter by target service.',
    'HELP_MISC_15': 'List async message producers by microservice, producer kind, or topic substring. Returns producer nodes (no resolve step).',
    'HELP_MISC_16': 'Filter by producer kind.',
    'HELP_MISC_17': 'Filter by topic substring.',
    'HELP_MISC_18': 'List message topics grouped by producer. No :Topic node exists; this command groups producers by topic name. --consumer-in resolves consumers (listener methods) via EXPOSES edges to Route(topic).',
    'HELP_MISC_19': 'List scheduled task symbols (capability=SCHEDULED_TASK). Returns Symbol nodes with the SCHEDULED_TASK capability.',
    'HELP_MISC_2': 'Print the SessionStart priming payload: what jrag is, the trust rule, live index state (freshness, services, symbol/route/client/producer counts, watch daemon liveness), and the command surface. Reads filesystem metadata only. Silent (rc 0, no output) when this repo has no jrag index — safe to install as a user-scope SessionStart hook. --hook-json wraps the payload in the Claude Code SessionStart envelope.',
    'HELP_MISC_20': 'List message listener symbols (capability=MESSAGE_LISTENER). Returns Symbol nodes with the MESSAGE_LISTENER capability.',
    'HELP_MISC_21': 'Filter by topic substring (on producer member).',
    'HELP_MISC_22': 'List JPA entity symbols (role=ENTITY). Returns Symbol nodes with the ENTITY role.',
    'HELP_MISC_23': 'Hint for resolve (omit for broad search).',
    'HELP_MISC_24': 'Resolve <query> then traverse the call graph inbound (who calls me?). Symbol -> g.find_callers (CALLS edges, --service/--module pushed down). Route -> g.find_route_callers; route callers are cross-service by construction, so --service narrows WHICH route resolves (a resolve-time filter) rather than filtering the resulting callers. --include-external controls whether external (JDK/Spring/Lombok) callers are excluded (default: excluded).',
    'HELP_MISC_25': "Symbol FQN/name (e.g. 'pkg.Svc#method(Arg)') or route path.",
    'HELP_MISC_26': 'Resolve <query> (Symbol) then traverse the call graph outbound (what do I call?). Calls g.find_callees; --include-external is symmetric with callers.',
    'HELP_MISC_27': "Symbol FQN/name (e.g. 'pkg.Svc#method(Arg)').",
    'HELP_MISC_28': 'Include external (JDK/Spring/Lombok) callees (default excluded).',
    'HELP_MISC_29': 'Resolve <query> (type Symbol) then walk EXTENDS/IMPLEMENTS both directions: out = supertypes (parents), in = subtypes (children). No --service/--module push-down (structural edges).',
    'HELP_MISC_3': 'Find nodes by query or filter. Two modes:\n  Query mode (positional <query>): search by name/FQN (symbols only); --fuzzy\n    falls back exact -> prefix -> substring when the exact match is empty.\n  Filter mode (no positional): apply structured filters (NodeFilter flags).\nKind inference: domain flags (--http-method, --client-kind, --producer-kind) imply\nroute/client/producer when --kind is omitted. Contradiction emits an error envelope.\nQuery mode + non-symbol kind (explicit or inferred) errors: name/FQN lookup only\nsearches symbols; drop the positional <query> and use filter mode for routes/clients/producers.',
    'HELP_MISC_30': 'Class/interface FQN or name.',
    'HELP_MISC_31': 'Resolve <query> (interface Symbol) then call g.find_implementors. --service/--module pushed down; --capability pushed down to the backend (find_implementors accepts a capability filter).',
    'HELP_MISC_32': 'Interface FQN or name.',
    'HELP_MISC_33': 'Filter implementors by capability.',
    'HELP_MISC_34': 'Resolve <query> (class Symbol) then call g.find_subclasses (EXTENDS inbound). --service/--module pushed down.',
    'HELP_MISC_35': 'Class FQN or name.',
    'HELP_MISC_36': "Resolve <query> (method Symbol) then neighbors_v2([id], 'out', ['OVERRIDES']). The stored OVERRIDES edge runs overrider -> declaration (subtype method -> supertype declared method), so 'out' dispatches UP the hierarchy.",
    'HELP_MISC_37': "Method FQN or name (e.g. 'pkg.Impl#method(Arg)').",
    'HELP_MISC_38': "Resolve <query> (method Symbol) then neighbors_v2([id], 'in', ['OVERRIDES']) (= virtual OVERRIDDEN_BY out). 'in' traverses the stored OVERRIDES edge backward, dispatching DOWN from declaration to overriders.",
    'HELP_MISC_39': "Method FQN or name (e.g. 'pkg.Iface#method(Arg)').",
    'HELP_MISC_4': 'Inspect a node by resolving a query (name/FQN) and returning its full details\nincluding edge_summary. Uses resolve_v2 internally; on ambiguous candidates,\nreturns them (no auto-pick). On not_found, returns an error envelope.',
    'HELP_MISC_40': 'Resolve <query> (type Symbol) then call g.find_injectors (INJECTS inbound: classes that inject this type). --service/--module pushed down.',
    'HELP_MISC_41': 'Type FQN or name.',
    'HELP_MISC_42': 'Resolve <query> then call g.impact_analysis (reverse closure over INJECTS+IMPLEMENTS+EXTENDS: who breaks if this changes). --service is a CLIENT-SIDE post-filter (impact_analysis has no microservice param); surfaced as a warnings[] entry.',
    'HELP_MISC_43': 'Symbol FQN or name.',
    'HELP_MISC_44': 'Closure depth (default 2).',
    'HELP_MISC_45': 'Resolve <query> (entrypoint Symbol) then call g.trace_flow. Walks CONTROLLER -> SERVICE/COMPONENT -> CLIENT/REPOSITORY/MAPPER stages via INJECTS+EXTENDS+IMPLEMENTS (optionally + CALLS hops). --service/--module pushed down; --depth clamped to 1..3.',
    'HELP_MISC_46': 'Entrypoint symbol FQN or name.',
    'HELP_MISC_47': 'Neighbour hop count per stage (clamped 1..3, default 2).',
    'HELP_MISC_48': 'Min CALLS confidence when --follow-calls is on.',
    'HELP_MISC_49': 'Include external types reached via the CALLS hop (default excluded).',
    'HELP_MISC_5': 'Search query (name/FQN).',
    'HELP_MISC_50': 'Resolve <query> to a Route then call g.trace_request_flow. Inbound = cross-service HTTP/async callers (Client/Producer two-hop); outbound = CALLS hops from the route handler. Intra-service is an INDEX-TIME data property: CALLS edges are intra-codebase by construction, and the query carries no microservice predicate, so the result reflects whatever the fixture indexed (no query-time constraint). --depth clamped to 1..8.',
    'HELP_MISC_51': "Route path (e.g. '/chat/assign') or Kafka topic name (e.g. 'banking.chat.compliance.review'). Resolved with hint_kind=route; kafka_topic Routes match on topic.",
    'HELP_MISC_52': 'Max CALLS hops (clamped 1..8, default 5).',
    'HELP_MISC_53': "Resolve <query> (type Symbol) then neighbors_v2([id], 'out', ['INJECTS']) = the types this class injects (its direct dependencies). INJECTS is Symbol -> Symbol (declaring type -> injected type), so 'out' traverses from the injector to its dependencies. --service/--module are NOT applied (INJECTS is a structural edge with no microservice predicate); they surface as warnings[]. --include-external is accepted for surface symmetry with callers/callees but is a warned no-op here (INJECTS has no external-exclusion analog at the neighbors_v2 layer).",
    'HELP_MISC_54': "Symbol FQN or name (e.g. 'pkg.Svc').",
    'HELP_MISC_55': 'Accepted for symmetry; warned no-op on dependencies (INJECTS is structural).',
    'HELP_MISC_56': "RESOLVE-FIRST EXCEPTION: the first positional is a microservice NAME (e.g. 'chat-core'), NOT a query — it is passed literally to list_clients/list_producers/find_route_callers; resolve_v2 is NEVER run on it.\n\nDirection (default --both): clients/producers in OTHER services targeting this service. HTTP via list_clients(target_service=<svc>) + async via find_route_callers on this service's topic Routes.\n--outbound: clients/producers IN this service. HTTP via list_clients(microservice=<svc>) + producers via list_producers(microservice=<svc>).\n--both: render both inbound and outbound sections.\n\n--http-method and --calls-service filter HTTP callers only (clients have a target_service; producers do not). Producers are KEPT under --calls-service so the async channel stays visible; a warnings[] entry is emitted when --calls-service bypasses producers.",
    'HELP_MISC_57': 'Filter HTTP callers by method (e.g. POST). Applies to clients only.',
    'HELP_MISC_58': 'Narrow to edges involving this other service. Outbound: clients with target_service == <svc> (producers kept with a warning — no service target on ASYNC channels). Inbound: callers from microservice == <svc>.',
    'HELP_MISC_59': "List all Symbol nodes whose declared location is in <file>. Calls find_symbols_in_file_range(graph, filename=<file>, start_line=1, end_line=2**31-1) — the start_line=1 is required (the backend returns [] for start_line<1). --limit caps the entry count (the file's symbol table is otherwise unbounded); truncated is set when more entries exist. --offset is rejected (the backend takes no offset).",
    'HELP_MISC_6': 'Hint for resolve (omitted for broad search).',
    'HELP_MISC_60': 'Parse <file> with tree-sitter via the language backend (backend_for(...).parse — parse_java for .java, parse_kotlin for .kt), walk its import nodes, and resolve each imported FQN via resolve_v2 against the graph. Returns one node per import: resolved graph Symbol when resolve_v2 hits, or an unresolved placeholder carrying the raw FQN otherwise. Static and wildcard imports are included (marked in the row). --offset is rejected.',
    'HELP_MISC_61': 'File path (POSIX-relative to source root, or absolute).',
    'HELP_MISC_62': 'List every microservice with its resolved type-symbol count. Calls g.microservice_counts(). Renders as a counts listing. An aggregate view: --service / --module / --limit are NOT accepted (rejected at parse time).',
    'HELP_MISC_63': 'Count resolved type Symbols (class/interface/enum/record/annotation) grouped by microservice or module. --by {{microservice,module}} selects the grouping axis (default microservice); --service / --module narrow the count to one service or module (filters, independent of --by).',
    'HELP_MISC_64': 'Report the dominant roles among resolved Symbols and the route framework distribution. --service narrows the role tally to one microservice.',
    'HELP_MISC_65': "Dispatch on the positional <subject>:\n  Route path (starts with '/')  -> trace_request_flow (same as `flow`).\n  Microservice name             -> routes + clients + producers bundle.\n  Topic string                  -> producers + consumers for the topic.\n--as {{microservice,route,topic}} overrides auto-detection.\nAuto-detection: starts with '/' -> route; matches a known microservice -> microservice; otherwise -> topic.",
    'HELP_MISC_66': "Semantic search via search_v2 over the Lance index (JVM source chunks). --hybrid enables vector+keyword hybrid. --offset paginates. --path-contains narrows by file path substring. Filters (NodeFilter flags) narrow results.\n\n--fuzzy is accepted as a no-op (search is inherently semantic; --fuzzy is implicit). It is kept registered so callers that pass it don't hit an argparse error, and is silently ignored.",
    'HELP_MISC_67': 'Natural-language search query.',
    'HELP_MISC_68': 'Narrow to chunks whose filename contains this substring.',
    'HELP_MISC_69': 'Accepted as a no-op (search is always semantic; --fuzzy is implicit).',
    'HELP_MISC_7': 'Post-filter by Java symbol kind.',
    'HELP_MISC_70': 'Page offset (passed to search_v2; paginated via +1-fetch).',
    'HELP_MISC_71': 'Rebuild the vocabulary index sidecar from the current Ladybug graph. The index is a search-optimized projection of Symbol nodes used for did-you-mean suggestions and external membership checks in absence diagnosis. Printed on success: symbol count and sidecar path.',
    'HELP_MISC_72': 'Long-lived daemon: watches the source tree for changes (reindexing vectors/graph on a debounce) and serves the read commands (search/find/inspect/callers/callees/flow) over a warm Unix socket so queries skip the cold-start model/graph load.\n\nLifecycle:\n  jrag watch             run in the foreground (Ctrl+C / SIGTERM to stop)\n  jrag watch --detach    start as a background daemon and return\n  jrag watch --status    report up/down + pid + socket + last reindex\n  jrag watch --stop      SIGTERM a running daemon (SIGKILL after 5s)\nOnly one daemon may run per index (project lock). --status/--stop do NOT acquire the lock.',
    'HELP_MISC_73': 'Index directory (Lance + LadybugDB + cocoindex state)',
    'HELP_MISC_74': 'First-time index creation. Refuses if the resolved index directory already contains a LadybugDB graph or Lance tables. Exit 2 on refusal.',
    'HELP_MISC_75': 'Interactive setup wizard that guides users through: Java source detection, embedding model selection, agent host configuration, agent-surface wiring (SessionStart prime hook on the cli surface, stdio MCP server entry on the mcp surface — no skill/agent files deployed), and YAML config generation. Use --non-interactive for CI/automation.',
    'HELP_MISC_76': "Post-upgrade refresh: brings the deployed surface's entry up to date — the MCP server command path on the mcp surface, the SessionStart prime hook command on the cli surface. If an index exists, also runs an incremental Lance + graph catch-up (same as `increment`). Use --dry-run to preview changes without writing. Pass --surface to switch between the mcp and cli surfaces (migrates artifacts + marker). Requires a prior `install` run.",
    'HELP_MISC_77': "Switch agent surface: 'cli' (jrag console-script + SessionStart prime hook, no files deployed) or 'mcp' (stdio MCP server entry, tools only, no skill/agent artifacts). Tears down the old surface's artifacts and deploys the new surface's (also rewrites the install marker). Omit to keep the current surface; on a TTY you'll be prompted.",
    'HELP_MISC_78': 'Runs cocoindex catch-up and incremental LadybugDB graph update. Use --vectors-only to skip graph update.',
    'HELP_MISC_79': 'Default: full Lance reprocess (cocoindex --full-reprocess) then full LadybugDB graph rebuild. Use --vectors-only or --graph-only to run a single phase (mutually exclusive).',
    'HELP_MISC_8': 'Post-filter by role.',
    'HELP_MISC_80': 'Run only the Lance/cocoindex full reprocess phase (no graph builder).',
    'HELP_MISC_81': 'Runs cocoindex drop, removes LadybugDB, and drops Lance tables. Requires --yes or TTY confirmation.',
    'HELP_MISC_9': 'Post-filter by FQN substring.',
}
