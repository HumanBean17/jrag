{
  "_comment": "Template --mcp-config payload for the jrag server. The corpus binding (the index dir jrag serves) is parameterized at run time by Plan 2's driver: it copies this template, substitutes ${JRAG_INDEX_DIR} with the absolute path to bench/indexes/<corpus>, and writes the per-run config. Plan 1 commits only this template (no real index path is fixed here).",
  "mcpServers": {
    "jrag": {
      "command": "python",
      "args": ["-m", "java_codebase_rag.mcp.server"],
      "env": {
        "JAVA_CODEBASE_RAG_INDEX_DIR": "${JRAG_INDEX_DIR}",
        "JAVA_CODEBASE_RAG_SOURCE_ROOT": "${JRAG_SOURCE_ROOT}"
      }
    }
  }
}
