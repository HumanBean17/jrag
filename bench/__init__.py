"""``bench`` — standalone benchmark package for the jrag effectiveness study.

Deliberately NOT part of the ``java_codebase_rag`` distribution. Imported as
``import bench.<module>``; tests put the repo root on ``sys.path`` via
``tests/bench/conftest.py``.
"""
